# Server-only URL used by src/app/api/v1/[...path]/route.ts
BACKEND_API_ORIGIN=http://localhost:8080

# Exact Supabase storage hostname used for product images, for example:
# xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_IMAGE_HOST=

# Optional proxy timeout in milliseconds
PROXY_TIMEOUT_MS=10000
